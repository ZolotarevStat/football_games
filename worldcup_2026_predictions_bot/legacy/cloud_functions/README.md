# Legacy Cloud Functions Experiment

These files are kept only as historical context from the webhook / Yandex Cloud Functions experiment.

The active MVP runtime is the Yandex Cloud VM polling worker:

```text
python -m wc_predictions_bot.polling
```

Do not use this directory for current deployments unless the webhook path is intentionally revived and re-tested.
